{
  "name": "calculator",
  "version": "0.1.1",
  "private": true,
  "description": "İstemci-taraflı, sıfır bağımlılık dört işlem hesap makinesi (eval/Function yasak — bkz. docs/05-architecture.md, docs/07-security.md)",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  }
}
