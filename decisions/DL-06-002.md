# DL-06-002: Faz 6 kalite kapısı onarımı — ürün tipi meta düzeltmesi

- Tarih: 2026-07-25
- Faz: 6 — UI/UX
- Mod: AUTOPILOT
- Karar: Faz 6 kalite kapısı `[tip:cli] Komut sözleşmesi` kontrolünde düşüyordu çünkü `state.product.type` tanımsızdı ve varsayılan olarak `cli`'ye çözülüyordu. Ürün fiilen tarayıcıda çalışan istemci-taraflı bir web hesap makinesi (`docs/00-idea.md`, `docs/06-uiux.md`: "Ürün tipi: Web") olduğundan, artefakta sahte bir komut sözleşmesi eklemek yerine `state.product.type = "web"` olarak düzeltildi. `docs/06-uiux.md` zaten web tipine uygun ekran/akış sözleşmesini (tek ekran, tuş düzeni, ana akışlar) eksiksiz içeriyordu — artefakt değişmedi, yalnız meta doğrulandı.
- Değerlendirilen alternatifler: (1) Artefakta uydurma bir "komut/CLI sözleşmesi" bölümü eklemek — reddedildi, ürünü yanlış tanımlar ve kanıt tiyatrosu olur; (2) `state.product.type = "web"` düzeltmesi (seçilen).
- Gerekçe: `product.type` hiçbir fazda elle set edilmemişti, kod tabanında CLI'ya işaret eden hiçbir şey yok (Faz 9 henüz `src/` doldurmadı); brief ve UI/UX artefaktı tutarlı biçimde "web" diyor.
- Riskler: Faz 9 kodlama başladığında ürün tipiyle tutarsız bir yapı (ör. CLI script) yazılırsa bu düzeltme geçersiz kalır — Faz 9/14 kapıları bunu tekrar yakalar.
- Geri alınabilirlik: Yüksek (yalnız state meta alanı; artefakt/kod değişmedi).
- İnsan onayı: Otomatik (AUTOPILOT — /pipeline-fix-gate headless onarım).
- Varsayım mı?: Hayır — `docs/00-idea.md` ve `docs/06-uiux.md` içeriğinden doğrudan doğrulandı.
