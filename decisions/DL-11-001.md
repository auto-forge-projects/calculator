# DL-11-001: Faz 11 — DOM sözleşme/smoke testleri + NFR-3 senaryosu

- Tarih: 2026-07-25
- Faz: 11 — Test (Faz 10 F4/NFR-3 yönlendirmesinin kapatılması)
- Mod: AUTOPILOT
- Karar: `tests/contract.test.js` eklendi — (a) `index.html` statik HTML↔JS sözleşme kontrolü (data-digit/data-op/id'ler), (b) elle yazılmış minimal DOM stub ile 3 smoke senaryosu (tıklama + klavye), (c) NFR-3 10-ardışık-işlem sol-sağ senaryosu. `src/app.js` DEĞİŞTİRİLMEDİ (test-only).
- Değerlendirilen alternatifler: (1) jsdom bağımlılığı eklemek — reddedildi (DL-05-001 sıfır-bağımlılık ilkesini bozar, LITE kapsamda gereksiz); (2) DOM adaptörünü ayrı fonksiyona çıkarıp test etmek — reddedildi (kod değişikliği gerektirir, minimal stub yeterli oldu); (3) seçilen: elle stub + tek modül-örneği (coverage doğruluğu için `?query` re-import yerine tek dinamik import + sahne-arası `#clear` sıfırlama).
- Gerekçe: Faz 10 (PR-1.md F4) DOM adaptörünün sıfır test kapsamına sahip olduğunu ve bir typo'nun 27 test yeşilken uygulamayı bozabileceğini tespit etti. Bu düzeltilmeden Release'e geçmek regresyon kör noktası bırakırdı.
- Riskler: Stub gerçek DOM'un tüm davranışını taklit etmez (ör. gerçek tarayıcı olay modeli) — yine de sözleşme (id/data-*) + temel etkileşim akışını doğrular; NFR-4 (responsive) görsel doğrulaması hâlâ kullanıcı gözlemine bağlı.
- Geri alınabilirlik: Yüksek (yalnız test dosyası).
- İnsan onayı: Otomatik (AUTOPILOT) — DL-10-001'in doğrudan gereği (F4/NFR-3 yönlendirmesi).
- Varsayım mı?: Hayır — doğrudan PR-1.md F4 ve docs/03 NFR-3'ten türetildi.
- Kanıt: `npm test` → 38/38 pass; coverage `src/app.js` %91,79 satır (önceki %76,56).
