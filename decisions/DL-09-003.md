# DL-09-003: REQ-001 — C tuşu key-span3, ÷ grid hizası düzeltmesi

- Tarih: 2026-07-25
- Faz: 9 — Development (↺ Yeni İhtiyaç: REQ-001)
- Mod: AUTOPILOT
- Karar: `index.html`'de `#clear` butonunun class'ı `key-span2` → `key-span3` yapıldı (3 sütun kaplar); `style.css`'e `.key-span3 { grid-column: span 3; }` eklendi. Sonuç: 1. satırda C artık sütun 1-3'ü kaplıyor, ÷ 4. sütuna düşüyor — diğer operatörlerle (×, −, +) aynı sütunda hizalı.
- Değerlendirilen alternatifler: (1) ÷'yi 4. sütuna taşımak için grid-column:4 ile sabitlemek — C'nin gerçek genişliğini (3 sütun) yansıtmadığından, tek bir buton genişlik değişince tekrar bozulurdu; span3 kullanıcı görünür genişliği doğru temsil ettiği için tercih edildi.
- Gerekçe: Kök neden C'nin yalnızca 2 sütun kaplaması, 4 sütunluk grid'de 1. satırda boş bir sütun bırakıp ÷'yi sola kaydırmasıydı.
- Riskler: Yok — saf CSS/markup değişikliği, davranış (JS) etkilenmedi.
- Geri alınabilirlik: Yüksek (tek class + tek CSS kuralı).
- İnsan onayı: Otomatik (AUTOPILOT) — kaynak REQ-001 kullanıcı talebi.
- Varsayım mı?: Hayır — doğrudan REQ-001 metninden ve regresyon testinden (tests/contract.test.js:42) türetildi.
